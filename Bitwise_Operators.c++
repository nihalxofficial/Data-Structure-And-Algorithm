#include<iostream>
using namespace std;

int main(){

    // int a = 4, b = 8;
    // cout << (a & b) << endl;

    // int a = 4, b = 8;
    // cout << (a | b) << endl;

    // int a = 4, b = 8;
    // cout << (a ^ b) << endl;


    int leftShift =  4 << 1;  // a*2^b
    cout << leftShift << endl;

    int rightShift = 10 >> 1; // a/2^b
    cout << rightShift << endl;


    return 0;
}